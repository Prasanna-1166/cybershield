Flask==3.0.3
Flask-SQLAlchemy==3.1.1
Flask-Login==0.6.3
Flask-WTF==1.2.1
Flask-Migrate==4.0.7
Flask-Limiter==3.8.0
python-dotenv==1.0.1
email-validator==2.2.0
WTForms==3.1.2

# Production WSGI server (see Procfile). Never use `python run.py` / the
# Flask dev server in production.
gunicorn==23.0.0

# PDF export for the personalized Cyber Safety Report (pure Python, no
# system-level dependencies like wkhtmltopdf/Cairo).
reportlab==5.0.1

# --- Database drivers ------------------------------------------------------
# SQLite needs no driver (built into Python) and is the default — nothing
# below is required for local dev or a simple demo deployment.
#
# Uncomment ONE of these if you set DATABASE_URL to point at a real
# database server instead of SQLite:
# psycopg2-binary==2.9.9      # PostgreSQL (recommended for production)
# PyMySQL==1.1.1              # MySQL, if you already have one set up
psycopg2-binary==2.9.9